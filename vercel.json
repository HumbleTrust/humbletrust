{
  "buildCommand": "cd ../design-reference && npm install && npm run build",
  "outputDirectory": "../design-reference/dist",
  "installCommand": "echo skip-root-install",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
