# HumbleTrust Web

React + Vite frontend for HumbleTrust.

```bash
npm install
npm run dev
npm run build
```

The production deployment is built by the repository root `vercel.json` from this `web/` directory.
